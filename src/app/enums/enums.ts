export enum COLORS{
    red,
    yellow,
    blue,
    white,
    black,
    grey
}

export enum PAYMENT_TYPE   {
    cash,
    credit
}
export enum DELIVERY_STATE{
    received,
    pending,
    cancelled
}
export enum PAYMENT_STATE{
    paid,
    pending
}